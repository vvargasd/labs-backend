pipeline {
    agent any
    tools {
        maven 'Maven'
        jdk 'JDK 1.8'
    }
    stages {
        stage ('Build') {
            steps {
                echo 'This is a minimal pipeline.'
            }
        }
    }
}